# 📊 Diagrama de Arquitectura

Visualización de cómo funciona cada opción.

---

## 🎯 Opción 2: CDN + Embed Simple

```
┌─────────────────────────────────────────────────────────────────┐
│                     USUARIO EN NAVEGADOR                        │
│                   (Landing de HubSpot)                          │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                ┌──────────────┴──────────────┐
                │ Carga HTML que contiene:   │
                │ 1. CSS (survey-styles.css) │
                │ 2. JS (bundle.min.js)      │
                │ 3. Config (webhook URL)    │
                └──────────────┬──────────────┘
                               │
                ┌──────────────▼──────────────┐
                │  CLOUDFLARE PAGES (CDN)     │
                │  https://survey-*.pages.dev │
                │                             │
                │  • survey-styles.css        │
                │  • survey-bundle.min.js     │
                │  • Cacheado globalmente     │
                └──────────────┬──────────────┘
                               │
                ┌──────────────▼──────────────┐
                │  USUARIO COMPLETA ENCUESTA │
                │  • Responde preguntas      │
                │  • Click "Enviar"          │
                └──────────────┬──────────────┘
                               │
                ┌──────────────▼──────────────┐
                │   N8N WEBHOOK (POST)        │
                │ https://n8n.openip.cl/...   │
                │                             │
                │  Recibe JSON con datos      │
                └──────────────┬──────────────┘
                               │
                ┌──────────────▼──────────────┐
                │  RESPUESTAS GUARDADAS       │
                │  • N8N dashboard            │
                │  • Export JSON/CSV          │
                └─────────────────────────────┘
```

### Flujo de Actualización (Opción 2)

```
┌──────────────────────┐
│  Editar encuesta     │
│  SurveyComponent.tsx │
└──────────────┬───────┘
               │
               ▼
┌──────────────────────┐
│  npm run build       │
│  (Compila React+JS)  │
└──────────────┬───────┘
               │
               ▼
┌──────────────────────┐
│  git push origin main│
│  (Sube a GitHub)     │
└──────────────┬───────┘
               │
               ▼
┌──────────────────────────────┐
│  Cloudflare auto-detecta push│
│  Redeploy automático (5 min) │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│  Landing se auto-actualiza   │
│  (Sin tocar HubSpot)         │
└──────────────────────────────┘
```

---

## 🎯 Opción 3: HubSpot Custom Module

```
┌──────────────────────────────────────────────────────────────┐
│              USUARIO EN LANDING HUBSPOT                      │
│  (Dragged módulo "Encuesta Apprecio")                        │
└────────────────────────┬─────────────────────────────────────┘
                         │
                ┌────────▼────────┐
                │  Módulo carga   │
                │  desde HubSpot   │
                │  módulo registry │
                └────────┬────────┘
                         │
        ┌────────────────┴────────────────┐
        │  HubSpot busca en Git repo:     │
        │  hubspot-deployment/            │
        │  OPCION3-CUSTOM-MODULE/         │
        │                                 │
        │  • meta.yaml (metadatos)        │
        │  • fields.json (campos UI)      │
        │  • module.html (template)       │
        │  • module.scss (styles)         │
        │  • module.js (lógica)           │
        └────────────────┬────────────────┘
                         │
        ┌────────────────▼────────────────┐
        │  HubSpot compila & renders      │
        │  • React 18                     │
        │  • SurveyJS                     │
        │  • Con configuración del módulo │
        └────────────────┬────────────────┘
                         │
        ┌────────────────▼────────────────┐
        │  Editor puede cambiar:          │
        │  • Logo (URL)                   │
        │  • Color primario               │
        │  • Webhook URL                  │
        │  • Textos botones               │
        │  (UI visual en HubSpot)         │
        └────────────────┬────────────────┘
                         │
        ┌────────────────▼────────────────┐
        │  Usuario completa encuesta      │
        └────────────────┬────────────────┘
                         │
        ┌────────────────▼────────────────┐
        │  Datos → N8N webhook (POST)     │
        └──────────────────────────────────┘
```

### Flujo de Actualización (Opción 3)

```
┌──────────────────────┐
│  Editar encuesta     │
│  SurveyComponent.tsx │
└──────────────┬───────┘
               │
               ▼
┌──────────────────────┐
│  npm run build       │
│  (Compila)           │
└──────────────┬───────┘
               │
               ▼
┌──────────────────────┐
│  git push origin main│
│  (Sube a GitHub)     │
└──────────────┬───────┘
               │
               ▼
┌──────────────────────────────┐
│  HubSpot auto-sincroniza     │
│  desde Git repo (5 min)      │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│  Módulo se actualiza en      │
│  TODAS las landings que lo   │
│  usan (automático)           │
└──────────────────────────────┘
```

---

## 🔄 Comparativa Visual

### Opción 2: CDN + Embed

```
Landing HubSpot
    │
    ├─ Header
    ├─ Contenido
    ├─ [Rich Text Module]
    │  └─ <div id="survey-container"></div>
    │     <link href="Cloudflare CDN/styles.css">
    │     <script src="Cloudflare CDN/bundle.js"></script>
    ├─ Footer
    
GitHub Repo → Cloudflare Pages (auto-deploy)
                      ↓
            assets/survey-*.min.* 
                      ↓
            Landing carga desde CDN
```

### Opción 3: Custom Module

```
Landing HubSpot
    │
    ├─ Header
    ├─ [Módulo: Encuesta Apprecio] ← Arrastra de módulos
    │  └─ Editar configuración (UI visual)
    │     • Logo
    │     • Color
    │     • Webhook
    │     • Textos
    ├─ Footer

GitHub Repo → HubSpot Git Sync (auto-deploy)
                      ↓
          hubspot-deployment/
          OPCION3-CUSTOM-MODULE/
                      ↓
          Módulo compilado en HubSpot
                      ↓
          Disponible en módulos reutilizable
```

---

## 📊 Componentes

### Archivos Necesarios

```
Opción 2: CDN + Embed
├─ dist/index.html              (landing standalone)
├─ dist/assets/
│  ├─ survey-bundle.min.js      (1.8M)
│  └─ survey-styles.css         (286K)
└─ CODIGO_EMBED_OPCION2.html    (HTML para pegar en HubSpot)

Opción 3: Custom Module
├─ hubspot-deployment/
│  └─ OPCION3-CUSTOM-MODULE/
│     ├─ meta.yaml              (metadatos)
│     ├─ fields.json            (campos UI)
│     ├─ module.html            (template)
│     ├─ module.scss            (estilos)
│     ├─ module.js              (lógica)
│     └─ package.json           (dependencias)
└─ (comparten) dist/assets/*
```

---

## 🌍 Flujo Global de Datos

```
┌─────────────────────────────────────────────────────────────┐
│                   USUARIO FINAL                              │
│              (Navegador, cualquier dispositivo)              │
└────────────┬────────────────────────────────────────────────┘
             │
             │ Entra a landing HubSpot
             │ (https://tu-landing.hubspot...)
             │
    ┌────────▼────────┐
    │  Landing carga  │  HubSpot CDN
    │  (HTML)         │  cachea globalmente
    └────────┬────────┘
             │
             │ Opción 2: Carga desde Cloudflare
             │ Opción 3: Carga desde HubSpot
             │
    ┌────────▼───────────────────────────────┐
    │  Encuesta renderiza en navegador       │
    │  • React crea componentes               │
    │  • SurveyJS dibuja interfaz             │
    │  • CSS hace responsive                  │
    └────────┬───────────────────────────────┘
             │
             │ Usuario interactúa
             │ • Responde preguntas
             │ • Navega entre páginas
             │ • Click "Enviar"
             │
    ┌────────▼──────────────────────────┐
    │  JavaScript colecta datos JSON     │
    │  {                                  │
    │    "edad": 28,                      │
    │    "genero": "Masculino",           │
    │    ...                              │
    │  }                                  │
    └────────┬──────────────────────────┘
             │
             │ POST request a N8N Webhook
             │ (https://n8n.openip.cl/webhook/...)
             │
    ┌────────▼──────────────────────────┐
    │  N8N recibe y procesa              │
    │  • Valida datos                    │
    │  • Guarda en DB                    │
    │  • Puede enviar a otros servicios  │
    └────────┬──────────────────────────┘
             │
    ┌────────▼──────────────────────────┐
    │  Respuestas disponibles            │
    │  • N8N dashboard                   │
    │  • Export JSON/CSV                 │
    │  • CRM (si integrado)              │
    └──────────────────────────────────┘
```

---

## 🎯 Comparativa de Arquitectura

```
┌─────────────────────────────────────────────────────────────┐
│                     OPCIÓN 2                                │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  GitHub Repo                                                │
│       ↓                                                      │
│  Cloudflare Pages (auto-build & deploy)                     │
│       ↓                                                      │
│  CDN Global (edge caching)                                  │
│       ↓                                                      │
│  Assets: JS + CSS                                           │
│       ↓                                                      │
│  Landing HubSpot (Rich Text HTML embed)                     │
│       ↓                                                      │
│  Navegador usuario (carga desde CDN)                        │
│       ↓                                                      │
│  N8N Webhook                                                │
│       ↓                                                      │
│  Respuestas guardadas                                       │
│                                                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                     OPCIÓN 3                                │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  GitHub Repo                                                │
│       ↓                                                      │
│  HubSpot Git Sync (auto-sync módulos)                       │
│       ↓                                                      │
│  HubSpot Module Registry                                    │
│       ↓                                                      │
│  Landing HubSpot (módulo drag & drop)                       │
│       ↓                                                      │
│  Navegador usuario (carga módulo compilado)                 │
│       ↓                                                      │
│  N8N Webhook                                                │
│       ↓                                                      │
│  Respuestas guardadas                                       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 📈 Infraestructura Necesaria

```
OPCIÓN 2: Minimal
┌────────────┐     ┌──────────────┐     ┌─────────┐
│  GitHub    │────▶│  Cloudflare  │────▶│ Usuario │
│  (repo)    │     │  Pages (CDN) │     │(browser)│
└────────────┘     └──────────────┘     └────┬────┘
                                              │
                                         ┌────▼─────┐
                                         │ N8N       │
                                         │ (webhook) │
                                         └───────────┘

OPCIÓN 3: Integrated
┌────────────┐     ┌─────────────┐     ┌─────────┐
│  GitHub    │────▶│  HubSpot    │────▶│ Usuario │
│  (repo)    │     │ (modules)   │     │(browser)│
└────────────┘     └─────────────┘     └────┬────┘
                                            │
                                       ┌────▼─────┐
                                       │ N8N       │
                                       │ (webhook) │
                                       └───────────┘
```

---

## 🎓 Resumen Arquitectónico

| Aspecto | Opción 2 | Opción 3 |
|---------|----------|----------|
| **Dónde se sirve JS/CSS** | Cloudflare CDN | HubSpot servers |
| **Dónde se configura** | HTML code | HubSpot UI |
| **Quién compila** | Cloudflare | HubSpot |
| **Caching** | Edge global | Edge HubSpot |
| **Latencia** | 50ms-500ms (global) | 200ms-1s (HubSpot) |
| **Reutilización** | Copy-paste HTML | Un módulo |
| **Velocidad setup** | 5 minutos | 20 minutos |
| **Complejidad** | Baja | Media |

